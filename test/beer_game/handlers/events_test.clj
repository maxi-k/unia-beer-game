(ns beer-game.handlers.events-test
  (:require [beer-game.handlers.events :as events]
            [beer-game.config :as config]
            [beer-game.store :as store]
            [clojure.test :refer :all]
            [clojure.data :as data]))
