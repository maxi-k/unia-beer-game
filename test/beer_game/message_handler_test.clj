(ns beer-game.message-handler-test
  "Tests for the [[beer-game.message-handler]]."
  (:require [beer-game.message-handler :refer :all]
            [beer-game.handlers.auth :as auth]
            [clojure.test :refer :all]))
